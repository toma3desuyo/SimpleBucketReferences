# 除算演算子

## 概要
数を割って商を得る演算。整数除算と浮動小数除算で挙動が変わる。

## 振る舞い
ゼロ除算は例外か特別値にする。整数除算の丸め方向（切り捨て/切り上げ/ゼロ方向）を把握する。

## 例
function divide(a, b):
  if b == 0: raise Error("division by zero")
  return a / b


## 注意点
精度が必要なら高精度計算や分数表現を使う。
