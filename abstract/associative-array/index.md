# 連想配列

## 概要
キーと値を結び付けるコレクション。キーで高速アクセスできる。

## 振る舞い
キーの等価性やハッシュの規則を明確にする。順序保証の有無は実装に依存し、仕様で伝える。

## 例
map = new Map()
map.set("key", "value")
value = map.get("key")


## 注意点
可変オブジェクトをキーにすると値変更で参照できなくなることがある。
