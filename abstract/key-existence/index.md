# キーの存在確認

## 概要
連想配列で指定キーが登録されているか調べる。値が偽でもキー自体があるかを区別できる。

## 振る舞い
平均 O(1) または O(log n)。見つからなかったときに自動追加するか、エラーにするかを呼び出し側で統一する。

## 例
function has_key(map, key):
  return map.contains(key)


## 注意点
null や空文字をキーに許すかを決め、ドキュメントに残す。
