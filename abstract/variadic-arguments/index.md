# 可変長引数

## 概要
呼び出し時に任意の個数の引数を受け取る仕組み。

## 振る舞い
必須引数と混在させる場合は順序を決め、デフォルト値との組み合わせを明確にする。

## 例
function sum_all(...numbers):
  total = 0
  for each n in numbers:
    total += n
  return total


## 注意点
受け取ったリストをそのまま保持すると外部の変更が影響するため、必要に応じてコピーする。
