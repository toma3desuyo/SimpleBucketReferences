# 引数の既定値

## 概要
省略された引数に自動で入るデフォルト値。呼び出しを簡略化しつつ契約を明確にする。

## 振る舞い
デフォルトが定義時に固定されるか、呼び出し毎に評価されるかは言語で異なる。ミュータブルな値をデフォルトにすると共有されるので避ける。

## 例
function greet(name = "guest"):
  return "hello " + name


## 注意点
必須引数と任意引数の順序や指定方法を統一し、読み手の誤解を防ぐ。
