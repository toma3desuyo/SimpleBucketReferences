# 非同期処理

## 概要
待ち時間の長い I/O などをブロックせず並行実行する仕組み。

## 振る舞い
タスクの状態（実行中/完了/失敗/キャンセル）を管理し、結果取得時に例外を正しく伝播させる。

## 例
function start_task(work):
  task = spawn(work)
  return task


## 注意点
共有データへの同時アクセスは排他やメッセージパッシングで調整する。
