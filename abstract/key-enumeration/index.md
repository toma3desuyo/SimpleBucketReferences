# キーの一覧取得

## 概要
連想配列に含まれる全てのキーを列挙する。順序保証の有無は実装に依存する。

## 振る舞い
反復中にマップを変更すると結果が不定になることがあるので、必要ならスナップショットを作って列挙する。

## 例
function keys(map):
  list = []
  for each pair in map:
    list.append(pair.key)
  return list


## 注意点
件数が多い場合はページングやストリームで分割して取り出す。
