# 変数の埋め込み

## 概要
テンプレート文字列に値を挿入して新しい文字列を作る。

## 振る舞い
プレースホルダの書式やエスケープ規則は言語ごとに異なる。未定義変数を空文字にするか例外にするかも実装次第なので注意する。

## 例
template = "Hello, {name}!"
result = interpolate(template, {"name": "World"})


## 注意点
HTML などに出力する場合は必ずエスケープし、インジェクションを防ぐ。
