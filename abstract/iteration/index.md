# 反復処理

## 概要
同じ処理を複数回実行する基本制御。回数指定、条件成立中、コレクション走査の3系統がある。

## 振る舞い
停止条件を明確にし、ループ内で必ず状態を進める。外部リソースを扱う場合はタイムアウトや例外処理を入れる。

## 例
function repeat(times, action):
  i = 0
  while i < times:
    action(i)
    i += 1


## 注意点
走査中にコレクションを変更すると順序が壊れることがある。必要ならコピーやスナップショットを使う。
