# 親クラスの参照

## 概要
子クラスから親クラスのメンバーや初期化を呼び出すこと。

## 振る舞い
親の初期化を先に実行し、その後で子の追加状態を設定する。オーバーライド時に親実装を呼ぶかどうかを契約として明示する。

## 例
class Child extends Base:
  function init():
    super.init()
    self.extra = 1


## 注意点
多重継承がある言語ではどの親を呼ぶかが曖昧にならないよう順序を確認する。
