# 自分自身の参照

## 概要
オブジェクトが自分自身を指す参照を持ち、再帰処理やリンク構造で利用する。

## 振る舞い
循環参照はメモリ解放を遅らせることがある。必要に応じて弱参照や明示的な解放手順を用意する。

## 例
class Node:
  function size():
    if self.next is None:
      return 1
    return 1 + self.next.size()


## 注意点
自己参照を含む構造をシリアライズする場合は循環検出が必要。
