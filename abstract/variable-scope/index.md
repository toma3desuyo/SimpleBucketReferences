# 変数の有効範囲

## 概要
変数や関数が参照できる範囲。ブロック・関数・モジュールなどの境界で決まる。

## 振る舞い
静的スコープが一般的だが、動的スコープを持つ言語もある。不要に広いスコープは衝突や副作用の原因になる。

## 例
function outer():
  value = 1
  function inner():
    return value
  return inner()


## 注意点
同名の変数をシャドーイングしないよう命名を工夫する。
