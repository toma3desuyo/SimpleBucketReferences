# 等価演算子

## 概要
2つの値が同じかどうかを判定する。値の同値性と参照の同一性を区別する言語もある。

## 振る舞い
浮動小数は誤差のため完全一致にならないことが多い。構造体はフィールド比較かID比較かを決める。

## 例
function equals(a, b):
  if type(a) != type(b): return false
  return a == b


## 注意点
ハッシュと等価判定の定義を揃え、コレクションで矛盾が出ないようにする。
