# 変数 / 関数の宣言

## 概要
名前を束縛し、値や手続きをプログラム内で使えるようにする行為。スコープと寿命は宣言位置で決まる。

## 振る舞い
再宣言可否、初期化の必須/任意、関数の巻き上げなどは言語によって異なるため、プロジェクトの方針を決めておく。

## 例
function main():
  counter = 0
  function inc(): counter += 1
  inc()


## 注意点
グローバル宣言は最小限にし、モジュールや名前空間で衝突を避ける。
